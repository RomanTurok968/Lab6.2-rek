#include <iostream>
#include <vector>
#include <cstdlib>
#include <ctime>
#include <climits> // дл€ INT_MIN ≥ INT_MAX
#include <Windows.h>
using namespace std;

// ‘ункц≥€ дл€ формуванн€ масиву
void сформуватићасив(vector<int>& масив, int ≥ндекс, int м≥н«наченн€, int макс«наченн€) {
    if (≥ндекс == масив.size()) return;
    масив[≥ндекс] = м≥н«наченн€ + rand() % (макс«наченн€ - м≥н«наченн€ + 1);
    сформуватићасив(масив, ≥ндекс + 1, м≥н«наченн€, макс«наченн€);
}

// –екурсивна функц≥€ дл€ знаходженн€ м≥н≥мального елемента
int знайтић≥н(const vector<int>& масив, int ≥ндекс, int м≥н«наченн€ = INT_MAX) {
    if (≥ндекс == масив.size()) return м≥н«наченн€;
    return знайтић≥н(масив, ≥ндекс + 1, min(м≥н«наченн€, масив[≥ндекс]));
}

// –екурсивна функц≥€ дл€ знаходженн€ максимального елемента
int знайтићакс(const vector<int>& масив, int ≥ндекс, int макс«наченн€ = INT_MIN) {
    if (≥ндекс == масив.size()) return макс«наченн€;
    return знайтићакс(масив, ≥ндекс + 1, max(макс«наченн€, масив[≥ндекс]));
}

// ‘ункц≥€ дл€ виведенн€ масиву
void вивестићасив(const vector<int>& масив, int ≥ндекс) {
    if (≥ндекс == масив.size()) {
        cout << endl;
        return;
    }
    cout << масив[≥ндекс] << " ";
    вивестићасив(масив, ≥ндекс + 1);
}

int main() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    srand(static_cast<unsigned>(time(0))); // ≤н≥ц≥ал≥зац≥€ генератора випадкових чисел

    const int –ќ«ћ≤– = 10;    // –озм≥р масиву
    const int ћ≤Ќ_«Ќј„≈ЌЌя = -50; // ћ≥н≥мальне значенн€ елемент≥в
    const int ћј —_«Ќј„≈ЌЌя = 50;  // ћаксимальне значенн€ елемент≥в

    vector<int> масив(–ќ«ћ≤–);

    // 1. ‘ормуванн€ масиву
    сформуватићасив(масив, 0, ћ≤Ќ_«Ќј„≈ЌЌя, ћј —_«Ќј„≈ЌЌя);

    // 2. ¬иведенн€ масиву
    cout << "ѕочатковий масив: ";
    вивестићасив(масив, 0);

    // 3. ќбчисленн€ м≥н≥мального та максимального елемент≥в
    int м≥н = знайтић≥н(масив, 0);
    int макс = знайтићакс(масив, 0);

    // 4. ¬иведенн€ результату
    cout << "—ума м≥н≥мального (" << м≥н << ") та максимального (" << макс << ") елемент≥в: " << м≥н + макс << endl;

    return 0;
}
